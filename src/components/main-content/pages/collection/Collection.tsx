import { FC } from "react"
import styled from "styled-components"

export const Collection: FC = () => {
  return (
    <Container>
      <div>Collection Collection Collection Collection Collection</div>
    </Container>
  )
}

const Container = styled.div`
  width: 100%;
  height: 100vh;
  margin: 64px;
`
